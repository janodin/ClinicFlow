SECRET_KEY=change-me
DEBUG=0
ALLOWED_HOSTS=your-domain.com,www.your-domain.com
DB_ENGINE=django.db.backends.postgresql
DB_NAME=clinicflow
DB_USER=clinicflow
DB_PASSWORD=change-me
DB_HOST=127.0.0.1
DB_PORT=5432
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=1
DEFAULT_FROM_EMAIL=ClinicFlow <noreply@your-domain.com>

# For local development, use the console backend (emails print to terminal):
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Facebook Messenger Integration
# Create an app at https://developers.facebook.com/ and get these values
MESSENGER_APP_ID=
MESSENGER_APP_SECRET=
MESSENGER_VERIFY_TOKEN=your-random-verify-token-here
MESSENGER_SESSION_TIMEOUT_MINUTES=30
